# 安全政策（Security Policy）

##  Supported Versions（受支持版本）

本仓库为内容型技能集合，采用持续更新模式。仅 **最新 `main` 分支** 与 **最新正式发布（Release）** 受安全支持。

| 版本       | 受支持状态 |
|-----------|-----------|
| latest (`main`) | ✅ 受支持 |
| 历史 Release | ❌ 不再维护 |

## 报告漏洞（Reporting a Vulnerability）

如果你在本仓库（含 `skills/`、`app/`、`prototype/`、`tools/` 中的代码与脚本）中发现安全漏洞，**请勿通过公开 Issue 披露**。

请通过以下私密渠道报告：

- GitHub Security Advisory（推荐）：在仓库页面选择 **Security → Report a vulnerability**，直接以私密方式提交给维护者。
- 邮箱：security@example.com（请替换为真实安全联系邮箱）

### 报告内容建议

为便于快速定位与修复，请尽可能提供：

1. 漏洞类型与受影响文件路径（如 `skills/<name>/scripts/...`）
2. 复现步骤（PoC）
3. 潜在影响与严重等级评估
4. 是否已存在公开利用或缓解方案

### 处理流程

1. **确认接收**：维护者将在 **72 小时内** 确认收到报告。
2. **初步评估**：在 **7 个工作日内** 完成严重等级评估并回复处理计划。
3. **修复与验证**：维护者在私密分支修复，必要时协商 CVE 与公告时间。
4. **公开披露**：修复合并且 Release 发布后，经 reporters 同意公开致谢与公告。

我们会尊重负责任的披露，并在修复后酌情致谢贡献者（除非你要求匿名）。

## 安全红线（项目约定）

本仓库在开发协作中遵循以下安全红线（详见 `openspec/AGENTS.md` 与贡献规范）：

- 代码与日志中**不得包含密钥、Token、密码**等凭据。
- 用户输入必须校验，防范 XSS / 注入等攻击。
- 技能脚本（`scripts/`）若调用外部网络或执行系统命令，须在 `SKILL.md` 中明确声明依赖与风险。
- 生产环境强制 HTTPS；依赖定期审计（`npm outdated` / `pip list --outdated`）。
