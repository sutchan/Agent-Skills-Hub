{
  "name": "agent-skills-hub-prototype",
  "version": "1.5.0",
  "private": true,
  "description": "Agent Skills Hub 高保真原型（Next.js 静态导出，可部署到腾讯云 EdgeOne）",
  "scripts": {
    "prebuild": "python3 build_site.py || python build_site.py || echo \"[prebuild] 跳过数据重新生成（未找到 python 或生成失败，使用已提交的数据文件）\"",
    "dev": "next dev",
    "build": "next build"
  },
  "dependencies": {
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
