# 2026-08-25 工作日志

## Agent-Skills-Hub 项目结构/质量审查
- 以"软件开发团队"专家主理人身份，建立团队 `software-agentskillhub-review`，分派架构师高见远做整体评审（部分工作流·分析评审）。
- 技术栈：Next.js 14.2 App Router（仓库根 `app/`）+ React 18.3 + TS 5.5；`tools/*.mjs` 构建脚本（零运行时依赖）；双部署 Vercel(.next)/EdgeOne(prototype)；`skills/`(1940) 为权威数据源，构建生成 `data/skills-data.json` + `skills-metrics.json`。
- 关键风险（已落盘 `deliverables/agentskillhub-review-2026-08-25.md`）：
  - 🔴 P0：`package.json` 缺 `dev`/`start` 脚本，但 README 要求 `npm run dev/start`（含 predev 注释）→ 快速上手路径断裂。
  - 🟡 P1：frontmatter 解析 + 13 类分类法在 build/validate/import/fix 多脚本重复实现，易漂移；无 CI 工作流但 validate 自述为门禁。
  - 🟡 P2：build 脚本冗余 `npm install`；`engines.node:"24.x"` 过激；根目录 12 个 `_tmp_*`/`_scan_headers` 临时脚本未入库；手写 YAML 解析稳健性有限。
  - 🟢 低：prototype 转发层疑似遗留、tsconfig `@/*` 死别名、GA4 ID 硬编码、.gitignore 失效白名单、README 版本滞后、popularity O(n²)。
- 用户未要求改代码，仅要结构化审查结论（已交付报告文件）。

## Agent-Skills-Hub 改进项实施（P0–P3）与遗留收尾
- 建立团队 `software-agentskillhub-improve`，工程师寇豆码实施全部 P0–P3（package.json 补 dev/start/predev、抽 `tools/lib` 共享模块 taxonomy+frontmatter 去重、新增 `.github/workflows/ci.yml` 门禁、清理 12 个根临时脚本、建 `frontmatter.test.mjs` 回归测试、`engines.node` 降级 22.x + `.nvmrc`、tsconfig/.gitignore/README 对齐），QA 严过关独立验证 PASS。顺带修正 `prototype-2/3` 的 `name` 重名导致 `skills-metrics.json` 去重丢指标的缺陷（键数 220→222）。
- 遗留收尾（本轮 `software-agentskillhub-improve2`，快速模式）：工程师修 ① `validate-skills.mjs` 文案「9 类」→「13 类」三处 ② `ci.yml` 新增 `node --test "tools/lib/*.test.mjs"` 门禁步骤 ③ README 导航表按 `data/skills-data.json` 真实计数刷新（合计 217→223，与正文 223 一致）。QA 独立验证 PASS。
- 明确未处理项：`skills/web-prototype/SKILL.md` 的 `od:` 字段损坏（意图不明、无工具消费、非本次引入）→ 留待维护者裁定，未擅自改动。

## Agent-Skills-Hub 第二轮：深度问题分析与改进
- 取消专家模式后，以默认能力对**未深挖区域**做分析：`skills/` 权威源数据质量扫描 + `app/` 前端 + `tools/*.py` + 构建脚本。
- 扫描结论（node 脚本临时生成、已清理）：223 技能；**无「其他」分类泄漏（0）**，分类体系健康。误报排除：`en_category` 69「未知」是硬编码英文集合猜错（真实值如 `Engineering Practice & Quality`/`Media & Multimedia`，validate 已用 taxonomy 真实集合通过）；短描述 69 是块标量 `|-` 解析假象。
- **真实可落地改进（3 项，已全部实施并验证）**：
  1. **数据一致性**：`agent-development`/`prototype-designer` 的 `name`（Title Case）≠ 目录名，违反 README 约定且与其余 222 个 kebab-case 风格不一致 → 对齐为 kebab-case（data 中旧名已消失、可见 222）。
  2. **CI 门禁增强**：`tools/validate-skills.mjs` 新增「name 须等于目录名」强制校验，将文档化约定 codify 为门禁防漂移。
  3. **构建正确性（原审查 F12）**：`build-skills-data.mjs` 的 popularity 用 `includes` 子串匹配导致假阳性（"api" 误命中 "graphql-api"）→ 抽离 `tools/lib/popularity.mjs`（`computePopularity` + 词边界正则 `(?<![\w-])name(?![\w-])`），加 `popularity.test.mjs`（6 用例）。metrics 总和由子串虚高降至 62（更准确）。
- 验证：validate 223 通过(0)；build data/prototype 均 0；`node --test "tools/lib/*.test.mjs"` 13/13 通过；JSON/语法 OK。
- 遗留：`coverage.py` 质量可接受未改；`web-prototype` 的 `od:` 仍留待维护者裁定。
